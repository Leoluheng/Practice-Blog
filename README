Hello, welcome to my blog practice.

Frontend: This project is using the html templates and js code from billvsme/Vmaig_blog with some modification as to the choice of template engine (Using JFina-built-in Backend template engine and Vue as front end template engin).
Backend: This project is constructed using JFinal - the opensource MVC Java Webdev framework.

==========================================NOTES==========================================
| ||||||| When updating user's avatar, depending on the server you use (in my case it's Jetty) one may highly possibly encouter error if the avatar exceeds server's default maxFormContentSize.
|         It is very easy to exceed the default content size. To fix it, create an xml file under WEB-INF directory and name it as jetty-web.xml (this is for jettry only) and add the following content to the file:
|         --FOR JETTY 6-----------------------------------------------------------------------------------------------------
|         <?xml version="1.0"?>    
|         <!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure//EN" "http://jetty.mortbay.org/configure.dtd">      
|         <Configure id="WebAppContext" class="org.mortbay.jetty.webapp.WebAppContext">    
|            <Set name="maxFormContentSize" type="int">-1</Set>    
|         </Configure>  
|         ------------------------------------------------------------------------------------------------------------------
|         --FOR JETTY 7-----------------------------------------------------------------------------------------------------
|         <?xml version="1.0"?>    
|         <!DOCTYPE Configure PUBLIC "-//Mort Bay Consulting//DTD Configure//EN" "http://jetty.mortbay.org/configure.dtd">    
|         <Configure id="WebAppContext" class="org.eclipse.jetty.webapp.WebAppContext">    
|            <Set name="maxFormContentSize" type="int">-1</Set>    
|         </Configure> 
|         ------------------------------------------------------------------------------------------------------------------
|         Alternatively, modify jetty server startup parameter using -Dorg.mortbay.jetty.Request.maxFormContentSize=9000 or google jetty/tomcat maxFormContentSize



This project is strictly non-profit.
Original project: https://github.com/billvsme/vmaig_blog

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!Web development is surely a lot of fun!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
